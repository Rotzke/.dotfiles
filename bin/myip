#!/bin/bash

if [ -f ~/.proxy.lock ]
then
    echo `curl -H 'Cache-Control: no-cache' -s -x socks://127.0.0.1:9050 ifconfig.co/json | cut -d'"' -f4,10 | sed 's/\"/\//g'`
elif
    [ ! -f ~/.proxy.lock ]
then
   echo `curl -H 'Cache-Control: no-cache' -s ifconfig.co`
fi

